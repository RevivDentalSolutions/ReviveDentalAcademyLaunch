FROM node:22-bookworm-slim

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev --ignore-scripts
COPY server ./server

ENV NODE_ENV=production
EXPOSE 3002
CMD ["node", "server/ai-server.js"]
