FROM node:22-bookworm-slim

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev --ignore-scripts
COPY server ./server

ENV NODE_ENV=production
EXPOSE 3001
CMD ["node", "server/stripe-server.js"]
